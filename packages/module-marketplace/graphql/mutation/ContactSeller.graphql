mutation ContactSellerBySellerId(
  $subject: String!
  $query: String!
  $sellerId: Int!
) {
  contactSellerBySellerId(
    subject: $subject
    query: $query
    sellerId: $sellerId
  ) {
    message
  }
}
