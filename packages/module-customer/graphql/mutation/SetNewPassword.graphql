mutation ResetPasswrod(
  $email: email
  $resetPasswordToken: resetPasswordToken
  $newPassword: newPassword
) {
  resetPassword(
    newPassword: $newPassword
    email: $email
    resetPasswordToken: $resetPasswordToken
  )
}
