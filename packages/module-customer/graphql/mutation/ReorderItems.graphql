#import "@packages/module-quote/graphql/fragments/_cart.graphql"
mutation ReorderItems($orderNumber: String!) {
  reorderItems(orderNumber: $orderNumber) {
    cart {
      ...cart
    }
    userInputErrors {
      code
      message
      path
    }
  }
}
