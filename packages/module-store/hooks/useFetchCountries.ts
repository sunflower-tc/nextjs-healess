// import { STORE_CONFIG, getLocalStorage } from '@store/local-storage';
// import { useDispatch } from 'react-redux';

/**
 * useFetchCountries
 */
// for later currently has issues
export const useFetchCountries = () => {
  // const dispatch = useDispatch();
  // const storeData = getLocalStorage(STORE_CONFIG, true);
  /**
   * Calling datatype issue on this code. so problem created to push code.
   */
  // if (storeData && isValidArray(storeData.countries)) {
  //   dispatch(setCountries({ countries: storeData }));
  // } else {
  //   const countries: Promise<CountriesQueryResult> = graphqlRequest({
  //     query: COUNTRIES_QUERY,
  //   });
  //   countries.then((country) => {
  //     dispatch(setCountries(country));
  //     storeData.countries = country.countries;
  //     // setLocalStorage(STORE_CONFIG, storeData)
  //   });
  // }
  // if (storeData && isValidArray(storeData.countries)) {
  //   dispatch(setCountries({ countries: storeData }));
  // } else {
  //   const countries: Promise<CountriesQueryResult> = graphqlRequest({
  //     query: COUNTRIES_QUERY,
  //   });
  //   countries.then((country) => {
  //     dispatch(setCountries(country));
  //     storeData.countries = country.countries;
  //     // setLocalStorage(STORE_CONFIG, storeData)
  //   });
  // }
};
